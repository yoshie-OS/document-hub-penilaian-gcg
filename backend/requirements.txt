# Core web framework (Flask is actually used, not FastAPI)
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# File processing
pandas>=2.0.0
openpyxl>=3.1.0

# Optional: Enhanced OCR support (if needed)
# pytesseract>=0.3.10
# opencv-python>=4.8.0
# pdf2image>=1.16.0
# Pillow>=10.0.0